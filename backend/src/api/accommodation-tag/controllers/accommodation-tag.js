'use strict';

/**
 * accommodation-tag controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::accommodation-tag.accommodation-tag');
